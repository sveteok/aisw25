/*
Create a new TypeScript file. Create two variables there:

firstName variable of the type string and give it a value
age variable of the type number and give it a value
Print the message "<firstName> is <age> years old!".

After the message, try to change the age to "twenty" (string) and print the message again. What happens?
*/
const firstName: string = "Svetlana";
let age: number = 25;
console.log(`${firstName} is ${age} years old!`);
//age = "twenty"; // Result: TypeScript error
